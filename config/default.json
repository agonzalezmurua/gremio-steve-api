{
  "app": {
    "port": 3010,
    "host": "localhost",
    "protocol": "http",
    "path": "",
    "url": "{protocol}://{host}:{port}{path}",
    "auth_token_url": "{protocol}://{host}:{port}{path}/"
  },
  "web": {
    "host": "localhost",
    "protocol": "http",
    "port": 3000,
    "auth_redirect_url": "{protocol}://{host}:{port}/auth/osu/callback",
    "url": "{protocol}://{host}:{port}"
  },
  "cors": {
    "allowed_origin": "http://localhost:3000"
  },
  "osu": {
    "base_url": "https://osu.ppy.sh",
    "auth_url": "{base_url}/oauth/authorize",
    "api": {
      "path": "/api/v2"
    }
  },
  "database": {
    "connection_string": "mongodb://{config.domain}/{config.name}",
    "domain": "localhost:27017",
    "name": "gremio-steve"
  }
}
